{
  "mcpServers": {
    "terraform": {
      "type": "stdio",
      "command": "npx",
      "args": ["-y", "terraform-mcp-server"],
      "env": {
        "TF_WORKSPACE_PATH": "${PWD}",
        "TF_LOG": "WARN"
      }
    }
  }
}
